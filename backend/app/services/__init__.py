"""Backend service layer for EdgeSenseAI.

Services keep decision logic out of FastAPI route handlers so the platform can evolve from deterministic prototype data into live market-data-backed recommendations.
"""
