[pytest]
markers =
    unit: fast pure logic tests with no app, DB, provider, or network startup
    service: service-level tests with mocked/fake dependencies
    contract: API response contract tests using FastAPI TestClient
    integration: DB/provider/workflow integration tests
    slow: tests that are expected to take longer than normal feedback-loop tests
addopts = --strict-markers
